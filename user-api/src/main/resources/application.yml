server:
  port: 8000

spring:
  application:
    name: user-api
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/springcloud-netflix?useSSL=false&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/